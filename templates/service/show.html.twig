{% extends 'base.html.twig' %}

{% block title %}Service{% endblock %}

{% block body %}
    <h1>Service - pulpit Admina</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service.id }}</td>
            </tr>
            <tr>
                <th>Nazwa usługi</th>
                <td>{{ service.serviceName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('service_index') }}">Powrót do listy usług</a>

    <a href="{{ path('service_edit', {'id': service.id}) }}">Edycja</a>

    {{ include('service/_delete_form.html.twig') }}
{% endblock %}
